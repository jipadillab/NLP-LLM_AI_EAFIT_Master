# ============================================================
# NLP & LLM Interactive Lab — EAFIT
# requirements.txt
# ============================================================
# Instalación local:   pip install -r requirements.txt
# Streamlit Cloud:      sube este archivo junto a main_app.py

# --- Core app ---
streamlit>=1.38,<2.0
pandas>=2.0,<3.0
numpy>=1.26,<2.0

# --- Visualización ---
plotly>=5.20,<6.0

# --- Groq API (modelos modernos: GPT-OSS, Qwen3, LLaMA, DeepSeek — todos gratis) ---
groq>=0.11,<1.0

# --- Anthropic (Claude) — OPCIONAL, solo para el módulo Comparador ---
# NOTA: Anthropic no tiene tier gratuito de API (a diferencia de Groq).
# Si no vas a usar Claude, puedes omitir este paquete sin romper el resto de la app.
anthropic>=0.34,<1.0

# --- Tokenización ---
tiktoken>=0.7,<1.0          # BPE (cl100k, referencia GPT-3.5/4)

# --- NLP clásico ---
nltk>=3.9,<4.0               # POS, NER, VADER sentiment, n-gramas

# --- Embeddings, TF-IDF y proyecciones ---
scikit-learn>=1.4,<2.0       # TF-IDF, cosine_similarity, TruncatedSVD (PCA-like)
sentence-transformers>=3.0,<4.0   # all-MiniLM-L6-v2 / bge-m3 / nomic-embed-text
einops>=0.7                  # requerido por nomic-embed-text (trust_remote_code)

# --- Modelos clásicos locales (GPT-2, BERT, RoBERTa) ---
transformers>=4.44,<5.0
torch>=2.2,<3.0              # backend CPU para transformers/sentence-transformers

# ============================================================
# NOTAS:
# - torch se instala en versión CPU por defecto desde PyPI; en
#   Streamlit Community Cloud esto es lo esperado (no hay GPU).
# - bge-m3 y nomic-embed-text se descargan la primera vez que se
#   usan (cientos de MB a ~2GB) — ten paciencia en la demo en vivo,
#   o pre-descárgalos antes de clase.
# - Si vas a desplegar solo el subconjunto de módulos que usan Groq
#   (sin modelos clásicos locales), puedes omitir `torch` y
#   `transformers` y comentar el módulo "Modelos Clásicos vs Modernos".
# ============================================================
