# ============================================================
# NLP & LLM Interactive Lab — EAFIT
# Dependencias verificadas — SIN PyTorch / torchvision / transformers
# ============================================================
streamlit>=1.50,<2.0
plotly>=5.24
pandas>=2.2
numpy>=1.26

# Cliente oficial de la API de Groq
groq>=1.3

# Tokenización BPE (GPT-2 / cl100k) — no depende de PyTorch
tiktoken>=0.8

# Tokenización WordPiece de BERT (implementación en Rust) — no depende de PyTorch
tokenizers>=0.20

# Embeddings densos vía ONNX Runtime — NO instala PyTorch ni torchvision
fastembed>=0.4

# TF-IDF, Bag of Words, similitud coseno, SVD
scikit-learn>=1.5

# POS tagging, NER, tokenización, análisis de sentimiento (VADER)
nltk>=3.9
